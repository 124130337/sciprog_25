#Practical 9

This branch contains the files for practical 9. There is one code to be run.

#Compiling the code

To compile the codes, do the following:

gcc -o main main.c

#Executing the code

To execute the code, do the following:

./main

#Output

The code will prompt the user to enter a file name, and then returns wether or not the matrix in the file is a magic square.

for example,

Enter file name: magic_square.txt
No. lines, 3
This square is magic. 
